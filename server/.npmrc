; Skip Puppeteer’s Chromium download on CI (saves ~300MB + long install; Render kills slow builds with status 143)
; Local dev: run `npx puppeteer browsers install chrome` if you need server-side scraping.
puppeteer_skip_download=true
puppeteer_skip_chromium_download=true
